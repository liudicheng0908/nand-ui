import dropdownMenu from '../dropdown/src/dropdownMenu.vue';

import { withInstall } from '@nand-ui/utils/withInstall';

export const NandDropdownMenu = withInstall(dropdownMenu);

export default NandDropdownMenu;
